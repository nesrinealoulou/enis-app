[ec2-docker]
<EC2_PUBLIC_IP>  # This will be replaced with the actual IP by Jenkins

[ec2-docker:vars]
ansible_ssh_private_key_file=<path_to_private_key>  # Jenkins will replace this with the actual path
ansible_user=ubuntu

